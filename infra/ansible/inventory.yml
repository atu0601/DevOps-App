all:
  vars:
    ansible_connection: docker
    ansible_become: false
    ansible_python_interpreter: /usr/bin/python3
    nginx_port: 80
  children:
    webservers:
      hosts:
        ansible-node1:
    appservers:
      hosts:
        ansible-node2:
